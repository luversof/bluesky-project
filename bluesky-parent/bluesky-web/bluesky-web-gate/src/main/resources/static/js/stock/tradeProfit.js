document.addEventListener("DOMContentLoaded",()=>{initializeTradeProfitForm()});function initializeTradeProfitForm(){document.getElementById("tradeProfitForm")&&setupHtmxEventListeners()}function formatDateTimeLocal(date){const year=date.getFullYear(),month=padZero(date.getMonth()+1),day=padZero(date.getDate()),hours=padZero(date.getHours()),minutes=padZero(date.getMinutes());return`${year}-${month}-${day}T${hours}:${minutes}`}function padZero(num){return num<10?"0"+num:String(num)}function setupHtmxEventListeners(){document.body.addEventListener("htmx:configRequest",event=>{const detail=event.detail;if(detail.parameters.accountIdList){let accountIds=detail.parameters.accountIdList;if(Array.isArray(accountIds)?accountIds=accountIds.map(id=>String(id).trim()).filter(id=>id.length>0):accountIds=String(accountIds).split(",").map(id=>id.trim()).filter(id=>id.length>0),accountIds.length>0){delete detail.parameters.accountIdList;for(let index=0;index<accountIds.length;index++)detail.parameters[`accountIdList[${index}]`]=accountIds[index]}else delete detail.parameters.accountIdList}if(detail.parameters.stockItemIdList){let stockItemIds=detail.parameters.stockItemIdList;if(Array.isArray(stockItemIds)?stockItemIds=stockItemIds.map(id=>String(id).trim()).filter(id=>id.length>0):stockItemIds=String(stockItemIds).split(",").map(id=>id.trim()).filter(id=>id.length>0),stockItemIds.length>0){delete detail.parameters.stockItemIdList;for(let index=0;index<stockItemIds.length;index++)detail.parameters[`stockItemIdList[${index}]`]=stockItemIds[index]}else delete detail.parameters.stockItemIdList}detail.parameters.startDate&&(detail.parameters.startDate=convertToIsoWithOffset(detail.parameters.startDate)),detail.parameters.endDate&&(detail.parameters.endDate=convertToIsoWithOffset(detail.parameters.endDate))}),document.body.addEventListener("htmx:afterSwap",event=>{console.log("Trade profit data loaded successfully")}),document.body.addEventListener("htmx:responseError",event=>{console.error("Failed to load trade profit data",event.detail),showError("\uB370\uC774\uD130 \uC870\uD68C \uC911 \uC624\uB958\uAC00 \uBC1C\uC0DD\uD588\uC2B5\uB2C8\uB2E4.")})}function convertToIsoWithOffset(dateTimeLocal){if(!dateTimeLocal)return"";const date=new Date(dateTimeLocal),offset="+09:00",year=date.getFullYear(),month=padZero(date.getMonth()+1),day=padZero(date.getDate()),hours=padZero(date.getHours()),minutes=padZero(date.getMinutes()),seconds=padZero(date.getSeconds());return`${year}-${month}-${day}T${hours}:${minutes}:${seconds}${offset}`}function showError(message){const resultDiv=document.getElementById("tradeProfitResult");resultDiv&&(resultDiv.innerHTML=`
		<div class="alert alert-error">
			<svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
				<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
			</svg>
			<span>${message}</span>
		</div>
	`)}window.tradeProfitModule={formatDateTimeLocal,convertToIsoWithOffset};
